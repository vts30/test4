# Replace <nexus-url> with your company Nexus server address
# Replace <base64> with: echo -n "username:password" | base64
@atruvia:registry=https://<nexus-url>/repository/npm-hosted/
//<nexus-url>/repository/npm-hosted/:_auth=${NPM_AUTH}
//<nexus-url>/repository/npm-hosted/:always-auth=true
